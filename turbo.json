{
  "pipeline": {
    "typings": {
      "dependsOn": ["^typings"],
      "outputs": ["types/**"]
    },
    "build": {
      "dependsOn": ["typings", "^build"],
      "outputs": ["dist/**", "types/**"]
    },
    "dev": {
      "dependsOn": ["typings"],
      "cache": false
    }
  }
}
